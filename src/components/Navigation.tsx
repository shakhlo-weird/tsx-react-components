import React from 'react';
import logo from "logo.png";

const Navigation = () => {
    return (
        <nav>
            <img src={logo} alt="my very own logo"/>
        </nav>
    );
};

export default Navigation;